<%= render "search_form" %>

<%= render(
  "results_info",
  songs: @songs,
  search_params: params[:search],
  option: params[:option]) %>

<%= render "results_list", songs: @songs %>

<%= paginate @songs %>
